// Point this at wherever you deploy /backend. For local development with
// `npm start` inside backend/, the default port is 4000.
const BACKEND_URL = window.SHOULDER_ROM_BACKEND_URL || "http://localhost:4000";
