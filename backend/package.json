{
  "name": "shoulder-rom-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Sync backend for the Shoulder Kinematics Capture app — lets the doctor portal read a session from a different device than the one used for capture.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^2.0.0"
  }
}
